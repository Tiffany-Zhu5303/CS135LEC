#include <iostream>
#include <string>
using namespace std;

int main(){
  string s;
  int number = 0;
  while(cin >> s){
    int converted_s = stoi(s);
    number = number + converted_s;
 }
  cout << number << endl;
  return 0;
}
